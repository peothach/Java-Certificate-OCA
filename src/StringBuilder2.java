public class StringBuilder2 {
    public static void main(String[] args) {
        java.lang.StringBuilder stringBuilder = new java.lang.StringBuilder();
        /**
         * StringBuild is not acceptable value null when call method append.
         * That because we have a complication error
         */
        //System.out.println(stringBuilder.append(null).length());
    }
}
